// ModalWindowModel does not need any extension from Backbone.Model
// because we only need to store a message in a variable
// I've still declared it so nobody gets confused.
var ModalWindowModel = Backbone.Model;